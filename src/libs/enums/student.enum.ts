export enum StudentStatus {
    ACTIVE = "active",
    INACTIVE = "inactive",
    BANNED = "banned"
  }
  
  export enum StudentType {
    FREE = "free",
    PREMIUM = "premium"
  }

  export enum UserRole {
    STUDENT = 'student',
    TEACHER = 'teacher',
    ADMIN = 'admin'
  }